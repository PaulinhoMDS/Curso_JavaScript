const num = [7,8,9]

num.push(10,11,12) // adicionando no final

num.unshift(1,2,3) // adicionando no inicio

num.splice (3, 0, 4, 5, 6,) // adicionando no meio, colocando: ponto inicial de onde iniciar,
// quantos deletar, o que vai adicionar 

console.log(num)
